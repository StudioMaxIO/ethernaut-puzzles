{
  "name": "ethernaut",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "call": "truffle exec scripts/call.js --network rinkeby",
    "delegatecall": "truffle exec scripts/delegatecall.js --network rinkeby",
    "flip": "truffle exec scripts/flip.js --network rinkeby",
    "force": "truffle exec scripts/force.js --network rinkeby",
    "guess": "truffle exec scripts/guess.js --network rinkeby",
    "king": "truffle exec scripts/king.js --network rinkeby",
    "store": "truffle exec scripts/store.js --network rinkeby",
    "vault": "truffle exec scripts/vault.js --network rinkeby"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.2",
    "@truffle/hdwallet-provider": "^2.0.12",
    "dotenv": "^16.0.1"
  }
}
